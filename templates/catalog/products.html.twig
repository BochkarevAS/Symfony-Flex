{% extends 'base.html.twig' %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}

    <div class="container js-product-module">

        <div class="js-new-rep-log-form-wrapper">
            {{ include('/catalog/products_form.html.twig') }}
        </div>

        <table class="table table-striped">
            <thead>
                <tr>
                    <th>Наименование</th>
                    <th>Цена</th>
                    <th>&nbsp;</th>
                </tr>
            </thead>
            <tbody>
            </tbody>
            <tfoot>
                <tr>
                    <th>Total</th>
                    <th class="js-total-weight"></th>
                    <td>&nbsp;</td>
                </tr>
            </tfoot>
        </table>
    </div>

{% endblock %}

{% block javascripts %}
    {{ parent() }}

    <script src="{{ asset('build/app.js') }}"></script>

    <script type="text/template" id="js-rep-log-row-template">
        <tr data-weight="<%= price %>">
            <td><%= name %></td>
            <td><%= price %></td>
            <td></td>
            <td>
                <a href="#" class="js-delete" data-url="<%= self %>">
                    <span class="fa fa-trash"></span>
                </a>
            </td>
        </tr>
    </script>

{% endblock %}
